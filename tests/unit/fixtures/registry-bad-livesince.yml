# Fixture — otherwise valid, but `live_since` is not a YYYY-MM-DD date. Must be
# rejected (fail-loud, same contract as a bad `template`), never pre-filled raw.
version: 1
tenants:
  typo:
    name: Typo Tenant
    status: active
    managed: sofra
    box: staging
    domain: typo.sofrapiwas.com
    domain_mode: subdomain
    db: tenant_typo
    live_since: "yesterday"
