# Fixture — otherwise valid, but `live_since` is a format-valid yet IMPOSSIBLE
# calendar date (Feb 31). Must be rejected by the round-trip refine (fail-loud,
# same contract as a bad `template`), never silently rolled over + pre-filled.
version: 1
tenants:
  typo:
    name: Typo Tenant
    status: active
    managed: sofra
    box: staging
    domain: typo.sofrapiwas.com
    domain_mode: subdomain
    db: tenant_typo
    live_since: "2026-02-31"
